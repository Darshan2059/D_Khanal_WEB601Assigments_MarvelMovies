import { PipeContentFilterPipe } from './pipe-content-filter.pipe';

describe('PipeContentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeContentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
